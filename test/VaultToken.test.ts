import { expect } from "chai";
import { ethers } from "hardhat";

describe("VaultToken", function () {
  it("Should deploy and return correct name and symbol", async function () {
    // Kịch bản test sẽ được viết tại đây theo kế hoạch
  });
});
