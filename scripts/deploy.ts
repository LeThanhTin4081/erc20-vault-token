import { ethers } from "hardhat";

async function main() {
  console.log("Bat dau deploy các contract (TV1)");
  // Code deploy sẽ viết ở đây
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
